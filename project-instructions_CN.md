# 项目演示说明

你需要在 Lab 15 向评分员演示你的项目。我们**一定会**检查（但不限于）你代码的以下功能。请仔细阅读并准备演示测试用例。

请记得在 Lab 15 演示结束后**立即**向 Sakai 提交你的代码。

## DHCP

1. 通过基础测试用例（GitHub 仓库中有相关内容）。使用**默认**配置设置（即 `start_ip`、`end_ip`、`subnet mask`），并用 Python 脚本创建一个带有交换机、多主机的拓扑。正确地为**所有**主机分配有效的 IP 地址。

2. **修改** `start_ip`、`end_ip` 和 `subnet mask`，并用 Python 脚本创建一个带有交换机、多主机的拓扑。正确地为所有主机分配**新的有效** IP 地址。

3. 假设 `start_ip` 和 `end_ip` 之间的可用 IP 数量为 **n**。用 Python 脚本创建一个带有交换机和 **m** 台主机的拓扑，其中 **m > n**。在此情况下，前 n 台主机被分配有效的 IP 地址，而剩余的 m-n 台主机则**无法获得** IP 地址。

## 最短路径切换

1. 通过基础测试用例（GitHub 仓库中可获得），确保所有主机均可被访问。用 `pingall` 来检查连通性。

   - **每次**网络拓扑发生变化（例如：添加交换机、添加链路等），在控制器控制台中打印当前拓扑结构以及任意两台交换机之间的最短路径（及长度）（例如：`s1 到 s2: s1 -> s3 -> s4 -> s2, 3 条边`）。
   - 拓扑创建完毕后，使用 `pingall` 验证所有主机之间的连接性。

2. 提供一个**复杂测试用例**

   - **你需要提供一张图来展示你自己测试用例的初始拓扑结构。** 图中内容应为图的图形可视化。主机和交换机作为图的节点，链路作为边。此外，你需要在图中**标注每对主机之间的最短路径**，以便与程序生成的结果进行比对。
   - 测试用例应包含**超过 6 台主机**、**超过 6 台交换机**以及**超过 10 条边**。
   - 测试用例应支持以下功能：使用 Python 脚本初始化拓扑（类似基础测试用例），并通过 Mininet CLI **动态修改**拓扑。
   - 这些变更应覆盖以下拓扑修改操作：`handle_switch_add`、`handle_switch_delete`、`handle_host_add`、`handle_link_add`、`handle_link_delete`、`handle_port_modify`。
   - 每次修改后，使用第三方库（如 **networkx**）打印当前拓扑结构，并在控制器控制台中打印任意两台交换机之间的最短路径（及长度）（例如：`s1 到 s2: s1 -> s3 -> s4 -> s2, 3 条边`）。

## 防火墙

1. 通过基础测试用例（GitHub 仓库中有相关内容）。

2. **复用**最短路径切换中的复杂测试用例。在交换机上**添加防火墙规则**，观察整个网络拓扑中的最短路径是否**正确更新**。你的测试用例应涵盖以下场景：**防火墙使得两台原本可达的主机变得不可达**。

## 提示

1. Mininet 有以下命令，你可能会觉得有用：

   - `MN > switch s1 stop/start`（停止/启动交换机 s1）
   - `MN > link h1 s1 down/up`（断开/恢复主机 h1 与交换机 s1 之间的链路）
   - `MN > sh ovs-ofctl mod-port s1 1 down`（使用 OpenFlow 禁用交换机 s1 上的端口 1）

2. `addHost` 不会在 Mininet CLI 中进行测试，— 它仅在网络拓扑初始化时被触发。然而，`switch stop/start`（交换机启停）、`link down/up`（链路断连/恢复）以及 `port on/off`（端口开关）将会在 Mininet CLI 中进行测试。

3. 使用 Python 脚本创建主机时，请参照 GitHub 上的基础测试用例，创建**无 IP 的主机**。例如：`h1 = self.addHost('h1', ip='no ip defined/8')`
